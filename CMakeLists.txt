# CMakeList.txt : Top-level CMake project file, do global configuration
# and include sub-projects here.
#
cmake_minimum_required (VERSION 2.8)

project ("zh_vm")

# Include sub-projects.
add_subdirectory ("zh_vm")
